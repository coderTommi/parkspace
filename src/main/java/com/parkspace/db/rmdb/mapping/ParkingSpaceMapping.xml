<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.parkspace.db.rmdb.dao.ParkingSpaceDao">
	<!-- 根据id查询得到一个停车位对象 -->
	<select id="getParkingSpace" parameterType="string" resultType="com.parkspace.db.rmdb.entity.ParkingSpace">
		select * from parkingspace where spaceno=#{spaceno}
	</select>
</mapper>